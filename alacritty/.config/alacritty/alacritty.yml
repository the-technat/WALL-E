# Technat's Alacritty Config (https://git.technat.ch)
# Configuration for Alacritty, the GPU enhanced terminal emulator.

# Import additional configuration files
import:
  # Import color scheme
  - ~/.config/alacritty/alacritty-themes/themes/Solarized-Dark.yml
  # - ~/.config/alacritty/alacritty-themes/themes/Solarized-Light.yml

# env:
  # TERM variable
  #
  # This value is used to set the `$TERM` environment variable for
  # each instance of Alacritty. If it is not present, alacritty will
  # check the local terminfo database and use `alacritty` if it is
  # available, otherwise `xterm-256color` is used.
  #TERM: alacritty

window:
  # Allow terminal applications to change Alacritty's window title.
  dynamic_title: true

  # Override the variant of the GTK theme. Commonly supported values are `dark`
  # and `light`. Set this to `None` to use the default theme variant.
  #gtk_theme_variant: None

scrolling:
  # Maximum number of lines in the scrollback buffer.
  # Specifying '0' will disable scrolling.
  history: 50000

  # Scrolling distance multiplier.
  #multiplier: 3

# Font configuration
font:
  normal:
    family: FiraCode Nerd Font
    style: Regular

  bold:
    family: FiraCode Nerd Font
    style: Bold

  italic:
    family: FiraCode Nerd Font
    style: Italic

  bold_italic:
    family: FiraCode Nerd Font
    style: Bold Italic

  size: 12

# If `true`, bold text is drawn using the bright color variants.
#draw_bold_text_with_bright_colors: false

selection:
  # This string contains all characters that are used as separators for
  # "semantic words" in Alacritty.
  #semantic_escape_chars: ",│`|:\"' ()[]{}<>\t"

  # When set to `true`, selected text will be copied to the primary clipboard.
  save_to_clipboard: true

cursor:
  style:
    # Values for `shape`:
    #   - ▇ Block
    #   - _ Underline
    #   - | Beam
    shape: Underline

  # If this is `true`, the cursor will be rendered as a hollow box when the
  # window is not focused.
  unfocused_hollow: false

# Live config reload (changes require restart)
live_config_reload: true

shell:
  program: /sbin/tmux
  args:
  - new-session
  - -A
  - -D
  - -s
  - main

key_bindings:
  - { key: V,    mods: Control, mode: ~Vi, action: Paste }
  - { key: Key2, mods: Control, action: IncreaseFontSize }
  - { key: Key1, mods: Control,  action: DecreaseFontSize }
